# app = app || {}
#
# movieGroupView = new app.allMoviesView({ collection: movieGroup})
#
# $("#allMovies").html(movieGroupView.render().el)
#
# movieRouter = new app.Router()

Backbone.history.start()
